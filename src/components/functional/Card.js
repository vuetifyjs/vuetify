import { createSimpleFunctional } from '../../util/helpers'

const Menu = createSimpleFunctional('card__menu')
const Stack = createSimpleFunctional('card__stack')
const Actions = createSimpleFunctional('card__actions')
const Text = createSimpleFunctional('card__text')
const TitleActions = createSimpleFunctional('card__title-actions')
const TitleText = createSimpleFunctional('card__title-text')

export {
  Menu,
  Stack,
  Actions,
  Text,
  TitleActions,
  TitleText
}