import { test } from '~util/testing'
import VSelect from '~components/selects/VSelect'

test('VSelect.js', ({ mount }) => {
  it('should be in an error state when required and blurred with empty', () => {
    console.log(VSelect)
  })
})
