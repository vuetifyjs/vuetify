import { test } from '~util/testing'
import VStepper from '~components/steppers/VStepper'
import VStepperContent from '~components/steppers/VStepperContent'

test('VStepper.js', ({ mount }) => {
  it('should handle dynamic steps', () => {
    const wrapper = mount(VStepper)
  })
})