import install from ''
import * as components from './components'
import * as directives from './directives'

const version = '__VUETIFY_VERSION__'

export {
  version,
  components,
  directives
}

export default {
  install,
  version,
  components,
  directives
}
