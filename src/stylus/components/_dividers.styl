@import '../bootstrap'
@import '../theme'

divider($material)
  border-color: $material.dividers
  
theme(divider, "divider")

.divider
  border-width: thin 0 0 0
  display: block
  flex: 1
  width: 100%

  &--inset
    margin-left: 72px
    width: calc(100% - 72px)