@import '../bootstrap'
@import '../theme'

.divider
  border: none
  display: block
  height: 1px
  flex: 1
  width: 100%

  &--inset
    margin-left: 72px
    width: calc(100% - 72px)

divider($material)
  background-color: $material.dividers

theme(divider, "divider")
