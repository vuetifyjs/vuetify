@import '../bootstrap'

.content
  flex: 1 1 auto
  max-width: 100%
  transition: .3s padding $transition.swing
  will-change: padding

  &--wrap
    flex: 1 0 auto
    display: flex
    max-width: 100%
