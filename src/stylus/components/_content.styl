.content
  display: flex
  flex: 1 1 auto
  flex-direction: column
  min-width: 0
  min-height: 0
  overflow-y: auto
  overflow-x: hidden
  
  // Used to make the container fill the entire height
  > .container
    flex: 1
  
  @media $display-breakpoints.xs-only
    padding: $grid-gutter-mobile