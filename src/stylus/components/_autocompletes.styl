@import '../bootstrap'
@import '../theme'

v-autocomplete($material)
  //

theme(v-autocomplete, 'v-autocomplete')

.v-autocomplete
  &--is-selecting-index
    input
      opacity: 0

  &.v-select.v-text-field
    input
      pointer-events: auto
