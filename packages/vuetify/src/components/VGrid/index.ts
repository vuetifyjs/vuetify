import './VGrid.sass'

import VContainer from './VContainer'
import VCol from './VCol'
import VRow from './VRow'

export {
  VContainer,
  VCol,
  VRow,
}

export default {
  $_vuetify_subcomponents: {
    VContainer,
    VCol,
    VRow,
  },
}
