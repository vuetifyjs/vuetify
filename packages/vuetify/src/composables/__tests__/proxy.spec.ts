import { reactive } from 'vue'
import { useValueProxy } from '../proxy'

describe('proxy.ts', () => {
  describe('useValueProxy', () => {
    it('should use prop value if defined', () => {
      // not implemented
    })

    it('should use internal value if prop not defined', () => {
      // not implemented
    })

    it('should emit new value', () => {
      // not implemented
    })
  })
})
