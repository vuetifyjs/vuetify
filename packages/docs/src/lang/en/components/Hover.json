{
  "header": "# Hover",
  "headerText": "The `v-hover` component provides a clean interface for handling hover states for any component.",
  "examples": {
    "usage": {
      "desc": "The `v-hover` component is a wrapper that should contain only one element, and can trigger an event when hovered over. In order for `v-hover` to work properly, either the `value` prop should be set to `true` or the wrapped element should contain `slot-scope=\"{ wrapper }\"`."
    },
    "disabled": {
      "header": "### Disabled",
      "desc": "The `disabled` prop disables the hover functionality."
    },
    "hoverList": {
      "header": "### Hover list",
      "desc": "hovering list"
    },
    "transition": {
      "header": "### Transitions",
      "desc": "Create highly customized components that respond to user interaction."
    }
  },
  "props": {
    "disabled": "Turns off hover functionality"
  },
  "scopedSlots": {
    "default": "The default scoped slot"
  }
}
