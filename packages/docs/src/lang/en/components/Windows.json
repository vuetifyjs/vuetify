{
  "heading": "# Windows",
  "headingText": "The `v-window` component provides the baseline functionality for transitioning content from 1 pane to another. Other components such as `v-tabs`, `v-carousel` and `v-stepper` utilize this component at their core.",
  "examples": {
    "usage": {
      "desc": "Designed to easily cycle through content, `v-window` provides a simple interface to create truly custom implementations."
    },
    "onboarding": {
      "heading": "### Onboarding",
      "desc": "`v-window` makes it easy to create custom components such as a different styled stepper."
    },
    "vertical": {
      "heading": "### Vertical",
      "desc": "`v-window` can be vertical. Vertical windows have Y axis transition instead of X axis transition."
    },
    "reverse": {
      "heading": "### Reverse",
      "desc": "Reverse `v-window` always displays reverse transition."
    },
    "account": {
      "heading": "### Account creation",
      "desc": "Create rich forms with smooth animations. `v-window` automatically tracks the current selection index to automatically change the transition direction. This can be manually controlled with the `reverse` prop."
    }
  },
  "props": {
    "activeClass": "Mixins.BaseItemGroup.props.activeClass",
    "continuous": "If `true`, window will \"wrap around\" from the last item to the first, and from the first item to the last",
    "nextIcon": "Icon used for the \"next\" button if `show-arrows` is `true`",
    "prevIcon": "Icon used for the \"prev\" button if `show-arrows` is `true`",
    "reverse": "Reverse the normal transition direction.",
    "reverseTransition": "The transition used when the component is reversing items. Can be one of the [built in transitions](/styles/transitions) or one your own.",
    "showArrows": "Display the \"next\" and \"prev\" buttons",
    "showArrowsOnHover": "Display the \"next\" and \"prev\" buttons on hover. `show-arrows` MUST ALSO be set.",
    "touch": "Provide a custom **left** and **right** function when swiped left or right.",
    "touchless": "Disable touch support.",
    "transition": "The transition used when the component progressing through items. Can be one of the [built in transitions](/styles/transitions) or one your own.",
    "vertical": "Uses a vertical transition when changing windows.",
    "v-window-item": {
      "disabled": "Prevents the item from becoming active when using the \"next\" and \"prev\" buttons or the `toggle` method",
      "reverse-transition": "Sets the reverse transition",
      "transition": "Mixins.Transitionable.props.transition",
      "value": "Mixins.Groupable.props.value"
    }
  },
  "events": {
    "change": "Emitted when the component value is changed by user interaction"
  }
}
