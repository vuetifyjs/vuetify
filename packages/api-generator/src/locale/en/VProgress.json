{
  "props": {
    "type": "Determines which progress indicator to render.",
    "label": "Text displayed alongside the progress indicator.",
    "labelPosition": "Controls the position of the label relative to the progress indicator.",
    "valueFormat": "Formatter for the `aria-valuetext`. Use `[value]` as a placeholder in strings, or pass a function that receives the current value and returns a string."
  },
  "slots": {
    "label": "Slot for custom label content."
  }
}
